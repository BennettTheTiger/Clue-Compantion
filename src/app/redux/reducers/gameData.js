import { DEFAULT_GAME } from '../../../data/defaultGame';

function gameData(state = DEFAULT_GAME, action) {
    switch(action.type){
        default:
            return state
    }
}

export default gameData;