import React from 'react';

const Checkbox = (props) => {
    return (
        <div
        className='clue-checkbox'
        >
            <input type='checkbox' />
        </div>
    )
}

export default Checkbox;